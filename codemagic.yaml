workflows:
  ios-capacitor:
    name: iOS Capacitor Build
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      node: 18.x
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build web assets
        script: |
          npm run build
      - name: Add and Sync iOS platform
        script: |
          npx cap add ios || true
          npx cap sync ios
      - name: Build iOS app
        script: |
          xcodebuild build -workspace ios/App/App.xcworkspace -scheme App -configuration Release -derivedDataPath build
    artifacts:
      - build/Build/Products/Release-iphoneos/*.app
      - build/Build/Products/Release-iphoneos/*.ipa